%h1 Listing filters

%table
  %thead
    %tr
      %th
      %th
      %th

  %tbody
    - @filters.each do |filter|
      %tr
        %td= link_to 'Show', filter
        %td= link_to 'Edit', edit_filter_path(filter)
        %td= link_to 'Destroy', filter, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Filter', new_filter_path
